level0:

1.
    1. le suid bit est celui qui permet a un programme en tant qu'user. il est dangeureux parce que nous sommes dans le level0, mais le programme execute en tant que level1. 
    2. Non, parce qu'on a besoin d'etre le user level1 pour lire le password du level1
    3. je ne sais pas
2.
    1.techniquement, en memoire le segfault occure, car on essaie d'acceder a un endroit de la memoire auquel il n'y a rien/ on a pas acces.
    2.la ligne 14 :   nb = atoi(argv[1]); Comme argv[1] n'exsite pas, ca provoque un segfau;t.
    3. avoir une condition pour s'il n'y a pas d'arguments.
3.
    1. je ne sais pas
    2. dans $eax. car cest le registre qui stocke par defaut les resultats?
    3. jne fait: jump if not equal. donc si %eax is not equal a 0x1a7 alors ca jump a l'adresse donnee, ici 0x9048f58
4.
    1. je ne sais pas faire. pas besoin de savoir faire, beaucoup de calculateurs en ligne le font 
    2. je ne sais pas, plus optimise?
    3. 3735928559, mais pas important
5.
    1. je ne sais pas explique moi
    2. je ne sais pas explique moi
    3. je ne sais pas , mais je ne crois pas si?
6.
    1. parce que le programme a les droits en tant level1 (le bit suid)
    2. il est lisible par user level1
    3. avec la commande whoami

level1:
1.
    1. Un buffer overflow est lorsqu'on depasse l'espace dedie en memoire dans un buffer. ex: on creer un string : char test[4]. si on affecte "123456" a la string test, on fait un buffer overflow.
    2. car c'est un buffer overflow. en regardant le code source.c dans ghidra, on voit que le buffer est assigne 76 pas 100.\
    3. ecraser eip permet de remplacer l'adresse de retour de la fonction main. quand la fontcion main va se terminer et arriver au return, au lieu de return a l'adress eip normale, elle ira a l'adresse qui a ecrase cette valeur
2.
    